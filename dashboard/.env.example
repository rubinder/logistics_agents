# Base URL of the logistics-agents API (baked in at BUILD time by Vite).
# Leave empty for same-origin. Example: https://api.example.com
VITE_API_BASE=
# Set to 1 to force the bundled sample data (standalone showcase with no backend).
VITE_USE_FIXTURES=
